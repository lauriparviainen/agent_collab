__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.tox/
.nox/
*.egg-info/
/build/
/dist/

# uv writes this when a reviewer (or anyone) runs `uv run` in the repo; the
# project resolves deps from pyproject.toml with pip, not uv, so the lockfile
# is not part of the workflow.
uv.lock

# Local Python environments.
.venv/
venv/
env/

# Local environment files. Keep intentional public examples visible.
.env
.env.*
!.env.example
!.env.*.example

# Coverage output.
.coverage
.coverage.*
htmlcov/
coverage.xml
coverage.json
lcov.info

# OS metadata.
.DS_Store
Thumbs.db

# Common private-key and certificate-container files.
*.key
*.pem
*.p12
*.pfx

.agents/
.codex/
.agent-collab/data/
.agent-collab/mcp-review-workdir/
.agent-collab/sessions/
