schema_version = 1

[options.model]
type = "string"
suggested = ["fable", "sonnet", "opus"]

[options.permission_mode]
type = "string"
allowed = ["default", "plan", "acceptEdits", "bypassPermissions"]

[options.thinking_level]
type = "string"
allowed = ["low", "medium", "high", "xhigh", "max"]

[options.thinking_budget_tokens]
type = "integer"
min = 0
max = 32768
